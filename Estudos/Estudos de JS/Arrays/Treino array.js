/*let valores = [8, 1, 7, 4, 2, 9]
console.log(valores)
for(let pos = 0; pos < valores.length; pos++) { //O código pos < valores.length - indica o valor (comprimento) dos valores.
  console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}
for(let pos in valores ){
  console.log(`A posição ${pos} tem o valor ${valores[pos]}`)
}
FOR = PARA
IN =  DENTRO

//////////////
Uma array é uma variável composta por vários elementos, cada elemento é composto pelo seu valor e uma chave de identeficação.
Toda array é um objeto
//////////////
*/
/*
 //valores.push(10) //Acrescenta um valor a uma array - Essa é a forma explicita. Precisa sempre está acima do .SORT
//valores[7] = 10 - Acrescenta um indice (posição) "xx" com o valor de 10. Essa é a forma automática 
//valores.length -  Cumpriemento do vetor. 
// valores.sort() - coloca os números em ordem crescente.
//valores.inderOf(xx) -  busca um valor dentro de um vetor, se ele pesquisar e não achar o valor irá aparecer o valor de -1

let valores = [5, 8, 2, 9, 3]
valores.sort()
console.log(valores)
console.log(`O vetor ${valores.length} posições`)
console.log(`O primeiro valor do vetor é ${valores[0]}`)
*/

